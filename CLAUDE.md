# Agent Usage Stat

See `AGENTS.md` for project architecture, commands, invariants, and where
documents belong. Plans and design notes go under `docs/plans/`, which is
gitignored and local, never at the repository root.
